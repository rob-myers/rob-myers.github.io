import BlogCounter from '@components/ui/blog-counter';
import EnvDemo from './env-demo';

<hr/>

# Methodology <BlogCounter date="1st September 2020" number="2" />

The engine has three parts:

- The _environment_ i.e. how the player sees & acts.
- The _geometry_ i.e. non-living structures like walls/doors/items.
- The _director_ i.e. that which controls the actors.

<EnvDemo envKey="b2-d1" />
