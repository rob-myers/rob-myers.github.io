import RootLayout from '@components/ui/root-layout';
import BlogMeta from '@blog/meta/meta';

const Meta: React.FC = () => {
  return (
    <RootLayout>
      <BlogMeta />
    </RootLayout>
  );
};

export default Meta;
