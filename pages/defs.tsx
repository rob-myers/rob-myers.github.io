import RootLayout from '@components/ui/root-layout';
import BlogDefs from '@blog/defs/defs';

const Defs: React.FC = () => {
  return (
    <RootLayout>
      <BlogDefs />
    </RootLayout>
  );
};

export default Defs;
