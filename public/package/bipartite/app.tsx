import * as React from 'react';
import css from './index.scss';

export const App: React.FC = () => {
  return (
    <div className={css.root}>
      Hello world!
    </div>
  );
};

export default App;
