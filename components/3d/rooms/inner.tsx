import { TransformProps } from "./room";
import { useEffect } from "react";

const Inner: React.FC<Props> = (props) => {
  useEffect(() => {
    
  }, []);

  return null;
};

type Props = TransformProps & {
  id: string;
}

export default Inner;
