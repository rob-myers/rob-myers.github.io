import { css } from "goober";

export default function SideNav() {

  return (
    <nav className={rootCss}>
      sidenav
    </nav>
  );
}

const rootCss = css`
  color: white;
  background: black;
`;